<div class="span6">
    <h4>E-mails</h4>
    {% if (personne.emails|length == 0) %}
        <div class="alert alert-info">
            <h4 class="alert-heading">Aucun résulat</h4>
            <p>
                Il n&apos;y a actuellement aucune adresse e-mail enregistr&eacute;e.
            </p>
        </div>
    {% else %}
        <table class="table">
            <thead>
            <tr>
                <th>Adresse e-mail</th>
                <th>Info</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            {% for email in personne.emails %}
                <tr>
                    <td>{{ email.adresse }}</td>
                    <td>{{ email.info }}</td>
                    <td style="text-align: right">
                        <a class="btn btn-mini" href="{{ path('pablo_email_edit', {'id' : email.id}) }}"><i class="icon-pencil"></i></a>
                        <a class="btn btn-mini btn-danger" href="{{ path('pablo_email_delete', {'id': email.id}) }}"><i class="icon-white icon-remove"></i></a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% endif %}
    <a class="btn" href="{{ path('pablo_email_add', {'id': personne.id}) }}">ajouter une adresse e-mail</a>
</div>