{% extends('PabloPeopleBundle::layout.html.twig') %}

{% block content %}
    <div class="page-header">
        <h2>&Eacute;diter une remarque</h2>
    </div>
    <form action="{{ path('pablo_comment_update', {'id': comment.id}) }}" method="post" enctype="{{ form_enctype(form) }}">
        {{ form_widget(form) }}
        <div class="form-actions">
            <input class="btn btn-primary" type="submit" value="valider">
            <a class="btn" href="{{ path('pablo_student_show', {'id': comment.person.id}) }}">annuler</a>
        </div>
    </form>
{% endblock content %}