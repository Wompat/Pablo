{% extends('PabloPeopleBundle::layout.html.twig') %}

{% block content %}
    <div class="page-header">
        <h2>Liste des adresses</h2>
    </div>
    {% if (not adresses) %}
        <div class="alert alert-info">
            <h4 class="alert-heading">Aucun r&eacute;sultat</h4>
            <p>
                Il n&apos;y aucune adresse enregistr&eacute; dans la base de donn&eacute;es.
            </p>
        </div>
    {% else %}
        <table class="table table-hover">
            <thead>
            <tr>
                <th>Nom</th>
                <th>Rue</th>
                <th>Num&eacute;ro</th>
                <th>Bo&icirc;te</th>
                <th>CP et localit&eacute;</th>
            </tr>
            </thead>
            <tbody>
            {% for adresse in adresses %}
                <tr>
                    <td>{{ adresse.personne.prenom|title ~ ' ' ~ adresse.personne.nom|upper }}</td>
                    <td>{{ adresse.voie }}</td>
                    <td>{{ adresse.numero }}</td>
                    <td>{{ adresse.boite }}</td>
                    <td>{{ adresse.localite }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% endif %}
{% endblock content %}